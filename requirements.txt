# QadamCare AI prototype runtime and research dependencies
# Python 3.11 or 3.12 is recommended. Pin exact versions before any reproducible release.
# Install only one OpenCV distribution in the environment. Do not install the unrelated
# `cv2` package or mix opencv-python with opencv-python-headless in the same environment.

torch
torchvision
opencv-python>=4.8,<5
albumentations
segmentation-models-pytorch
matplotlib
pandas
scikit-learn
tqdm
notebook
ipykernel
pyyaml
streamlit
pillow
reportlab
requests
numpy
